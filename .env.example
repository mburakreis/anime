# MyAnimeList API credentials — get these at https://myanimelist.net/apiconfig
MAL_CLIENT_ID=your_client_id_here
MAL_CLIENT_SECRET=your_client_secret_here

# Optional: override auto-detected redirect URI (e.g. for custom domain)
# MAL_REDIRECT_URI=https://your-domain.com/api/auth/callback
